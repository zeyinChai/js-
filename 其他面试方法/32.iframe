什么是 iframe
    1. iframe是html元素，用于在网页中嵌另一个网页
    2. iframe默认有一个宽高，存在边界
    3. iframe是个行内块级元素，可以通过display修改

页面样式风格不统一
顶层页面和内嵌页面的样式风格迥异，页面看起来不美观，没有项目的一体感，用户体验下降。
阻塞顶层页面的 onload 事件
内嵌页面加载完毕之后，主页面才加载完毕。
共享连接池
顶层页面和内层页面共享连接池，在 chrome 下同时只能发送 6 个 http 请求，iframe 的嵌入会影响主页面的资源加载。
https放http的iframe会请求不到